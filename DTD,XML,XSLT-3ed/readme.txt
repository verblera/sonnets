This folder contains:
- seven xml files
- one DTD files
- one XSLT file

DTD defines and check structure of XML files
XSLT file defines how XML should be presented 
each XML is marked up.

The third XSLT file creates a table where on the left
the rhymed words are grouped as they are rhymed between each other;
on the right their parts of speech is stated. 