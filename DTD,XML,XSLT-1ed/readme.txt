This folder contains:
- seven xml files
- one DTD files
- one XSLT file

DTD defines and check structure of XML files
XSLT file defines how XML should be presented 
each XML is marked up.

This XSLT file makes a short text from a few marked up elements
and structures a sonnet in a regular way: author, title, stanzas.